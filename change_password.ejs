<% layout('layout') -%>
<% if (error) { %><div class="alert err"><%= error %></div><% } %>
<% if (ok) { %><div class="alert ok"><%= ok %></div><% } %>
<form method="post" action="/profile/change-password">
  <div class="row">
    <div><label>Nouveau mot de passe</label><input type="password" name="password" required minlength="6"/></div>
    <div><label>Confirmer</label><input type="password" name="confirm" required minlength="6"/></div>
  </div>
  <div style="margin-top:12px; display:flex; justify-content:flex-end; gap:8px"><button class="btn primary">Mettre à jour</button></div>
</form>
