<!DOCTYPE html>
<html>
<head><title>Admin</title></head>
<body style="background:#1e3a8a; color:white;">
    <h1>Panneau d'administration</h1>
    <table border="1" style="background:white; color:black;">
        <tr><th>ID</th><th>Matricule</th><th>Role</th></tr>
        <% users.forEach(user => { %>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.matricule %></td>
                <td><%= user.role %></td>
            </tr>
        <% }) %>
    </table>
    <a href="/dashboard">Retour</a>
</body>
</html>
