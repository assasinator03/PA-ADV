
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title><%= title %> — Police-Academy-ADV</title>
  <link rel="stylesheet" href="/css/styles.css"/>
  <link rel="icon" href="/images/logo.png"/>
</head>
<body>
<div class="container-app">
  <aside class="sidebar">
    <div class="brand">
      <img src="/images/logo.png" alt="logo"/>
      <div>
        <h1 style="font-size:16px;margin:0">Police-Academy-ADV</h1>
        <small style="opacity:.7">Los Santos Police Department</small>
      </div>
    </div>
    <% if (user) { %>
    <nav class="nav" style="margin-top:12px">
      <a href="/" class="<%= active==='home'?'active':'' %>">Accueil</a>
      <a href="/agents" class="<%= active==='agents'?'active':'' %>">Liste du personnel</a>
      <a href="/history" class="<%= active==='history'?'active':'' %>">Historique</a>
      <% if (user.role==='ADMIN') { %><a href="/admin" class="<%= active==='admin'?'active':'' %>">Administration</a><% } %>
      <a href="/profile" class="<%= active==='profile'?'active':'' %>">Profil</a>
      <a href="/logout">Déconnexion</a>
    </nav>
    <% } %>
  </aside>
  <main class="main">
    <div class="header">
      <h2 style="margin:0"><%= title %></h2>
      <% if (user) { %><div class="badge-rank">Connecté : <strong><%= user.username %></strong> — Rôle: <%= user.role %></div><% } %>
    </div>
    <section class="card"><%- body %></section>
    <div class="footer">© <%= new Date().getFullYear() %> Police-Academy-ADV</div>
  </main>
</div>
</body>
</html>
