<!DOCTYPE html>
<html>
<head>
  <title>Login</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-blue-900 flex items-center justify-center h-screen">
  <div class="bg-white p-8 rounded shadow w-96">
    <h1 class="text-xl font-bold mb-4 text-center">Police Academy ADV</h1>
    <% if (error) { %>
      <p class="text-red-500 mb-2"><%= error %></p>
    <% } %>
    <form method="POST" action="/login">
      <input type="text" name="matricule" placeholder="Matricule" class="border p-2 w-full mb-3" required>
      <input type="password" name="password" placeholder="Mot de passe" class="border p-2 w-full mb-3" required>
      <button type="submit" class="bg-blue-600 text-white px-4 py-2 w-full">Se connecter</button>
    </form>
  </div>
</body>
</html>